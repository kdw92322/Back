<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.back.mapper.user.UserMapper">

    <select id="selectUserList" parameterType="map" resultType="map">
        SELECT a.id
              ,a.name
              ,a.role
              ,a.email
              ,COALESCE(a.status,'IA') as status
              ,a.phone
          FROM user_info a
         WHERE 1=1
         <if test="id != null">
            AND a.id = #{id}
         </if>
         <if test="name != null">
            AND a.name = #{name}
         </if> 
    </select>

    <insert id="insert" parameterType="java.util.Map">
        INSERT INTO user_info (
            id,
            name,
            password,
            email,
            phone,
            role,
            status,
            reg_date
        ) VALUES (
            #{id},
            #{name},
            #{password},
            #{email},
            #{phone},
            #{role},
            COALESCE(#{status},'A'),
            NOW()
        )
    </insert>

    <update id="update" parameterType="java.util.Map">
        UPDATE user_info
        SET name = #{userName},
            password = #{password},
            email = #{email},
            phone = #{phone},
            role = #{role},
            status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="java.util.Map">
        DELETE 
          FROM user_info
         WHERE id = #{id}
    </delete>
</mapper>